import { CSSProperties } from "react"
import center from "./data"

const image = center.map((v, i) => {
    center[i].gymPhotoSmall;
});

const img = (props: image) => {



    const imgCSS: CSSProperties {
        width: "50vw",
        height: "70vh",
        <img src= alt="" />
    }


    return <div style={}></div>
}